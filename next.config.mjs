/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'www.google.com', 
      'via.placeholder.com', 
      'm.media-amazon.com', 
      'in.danielwellington.com', 
      'mokobara.com',
    ],
}
}

export default nextConfig;
